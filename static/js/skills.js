"use strict";
(function () { const $ = id => document.getElementById(id); const esc = v => String(v ?? "").replace(/[&<>"']/g, c => ({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[c])); const source = s => s === "AI_suggested" ? "AI Suggested" : s === "manually_added" ? "Manual" : "Resume"; function render(items) { $("skill-count").textContent = items.length; $("skills-table").innerHTML = items.length ? `<div class="table-wrap"><table class="data-table"><thead><tr><th>Skill</th><th>Proficiency</th><th>Source</th><th>Added</th><th></th></tr></thead><tbody>${items.map(x => `<tr><td><strong>${esc(x.name)}</strong><small>${esc(x.category)}</small></td><td><div class="skill-bar"><div class="progress"><span style="width:${x.proficiency}%"></span></div><small>${x.proficiency}%</small></div></td><td><span class="pill indigo">${source(x.source)}</span></td><td>${x.created_at ? new Date(x.created_at).toLocaleDateString() : "—"}</td><td><button class="btn btn-danger btn-sm" data-remove="${x.id}">Remove</button></td></tr>`).join("")}</tbody></table></div>` : '<div class="empty-state"><h3>Your skill profile is empty</h3><p>Add the skills you already use to unlock meaningful gaps and roadmaps.</p></div>'; $("skills-state").hidden = true; $("skills-table").hidden = false; document.querySelectorAll("[data-remove]").forEach(btn => btn.addEventListener("click", async () => { try { const result = await ccApi.delete(`/api/skills/${btn.dataset.remove}`); render(result.data.skills); ccToast?.("Skill removed"); } catch (e) { ccToast?.(e.message, "error"); } })); } async function load() { try { render((await ccApi.get("/api/skills")).data.skills); } catch (e) { $("skills-state").className = "form-error"; $("skills-state").textContent = e.message; } } $("skill-proficiency")?.addEventListener("input", e => $("proficiency-value").textContent = e.target.value); $("skill-form")?.addEventListener("submit", async e => { e.preventDefault(); try { const result = await ccApi.post("/api/skills", { name: $("skill-name").value, proficiency: Number($("skill-proficiency").value), source: "manually_added" }); render(result.data.skills); e.target.reset(); $("skill-proficiency").value = 40; $("proficiency-value").textContent = 40; ccToast?.("Skill added"); } catch (error) { ccToast?.(error.message, "error"); } }); load(); })();
