"use strict";
(function () { const esc = v => String(v ?? "").replace(/[&<>"']/g, c => ({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[c])); const table = items => items.length ? `<div class="table-wrap"><table class="data-table"><thead><tr><th>Date</th><th>Resume</th><th>Role</th><th>ATS score</th></tr></thead><tbody>${items.map(x => `<tr><td>${new Date(x.created_at).toLocaleDateString()}</td><td>${esc(x.resume_name)}</td><td>${esc(x.target_role)}</td><td><strong>${x.ats_score}</strong></td></tr>`).join("")}</tbody></table></div>` : '<div class="empty-state"><h3>No history yet</h3><p>Your ATS and activity history will appear after your first upload.</p></div>'; async function load() { try { const d = (await ccApi.get("/api/history")).data; document.getElementById("history-content").innerHTML = table(d.ats_history); document.getElementById("activity-content").innerHTML = d.activities.length ? `<ul class="data-list">${d.activities.map(x => `<li class="data-row"><span><strong>${esc(x.message)}</strong><small>${new Date(x.created_at).toLocaleString()}</small></span></li>`).join("")}</ul>` : '<div class="empty-state"><h3>No activity yet</h3><p>Complete a career action to create your timeline.</p></div>'; } catch (e) { document.getElementById("history-content").textContent = e.message; } } load(); })();
