"use strict";
(function () { const history = document.getElementById("coach-history"); const esc = v => String(v ?? "").replace(/[&<>"']/g, c => ({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[c])); function render(items) { history.innerHTML = items.length ? items.map(x => `<div class="data-row"><span><strong>${x.role === "user" ? "You" : "Coach"}</strong><small>${esc(x.content)}</small></span></div>`).join("") : '<div class="empty-state"><h3>Start a coaching conversation</h3><p>Ask for a practical next step based on your saved career data.</p></div>'; } async function load() { try { render((await ccApi.get("/api/coach")).data.history); } catch (e) { history.textContent = e.message; } } document.getElementById("coach-form")?.addEventListener("submit", async e => { e.preventDefault(); const input = document.getElementById("coach-message"); try { const result = await ccApi.post("/api/coach", { message: input.value }); render(result.data.history); input.value = ""; } catch (error) { const box = document.getElementById("coach-error"); box.textContent = error.message; box.hidden = false; } }); load(); })();
